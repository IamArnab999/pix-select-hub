# 🖼️ Image Search & Multi-Select App (MERN + OAuth)

A full-stack image search web application built with the **MERN stack** and **OAuth authentication**, allowing users to log in, search images from the Unsplash API, select multiple images, and view their personal and global search history.

---

## 🚀 Features

- 🔐 **OAuth Authentication** — Login via **Google**, **Facebook**, or **GitHub**
- 🔍 **Image Search** — Fetch real images from **Unsplash API**
- 🖱️ **Multi-Select Grid** — Select multiple images using checkboxes
- 🧮 **Dynamic Counter** — Displays “Selected: X images”
- 🏆 **Top Searches Banner** — Shows top 5 most searched terms across all users
- 🕓 **User Search History** — Displays each user’s personal search history
- 💾 **MongoDB Integration** — Stores user searches and metadata
- ☁️ **Deployed on Lovable Cloud** — Backend + Database + Auth handled automatically

---

## 🧩 Tech Stack

| Layer | Technology |
|-------|-------------|
| **Frontend** | React.js, Tailwind CSS |
| **Backend** | Node.js, Express.js |
| **Database** | MongoDB (via Lovable Cloud) |
| **Authentication** | OAuth (Google, Facebook, GitHub using Passport.js) |
| **API Integration** | Unsplash Search API |
| **Deployment** | Lovable Cloud (Auto-Deployed) |

---

## 📂 Project Structure

